*.o
*~

# Assembled output (keep source .asm files)
*.rel
*.sym

# Executables
src/altair_emu
src/cpmemu
src/cpm_bios
src/test_disk
web/cpm_cli

# Web build output (generated from source)
web/cpm.js
web/cpm.wasm
web/cpm.data
# Note: web/bios.sys is tracked in git because um80 assembler is not available in CI

# Emscripten SDK (if installed locally)
emsdk/
